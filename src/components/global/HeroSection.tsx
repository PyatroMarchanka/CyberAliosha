import React from 'react';
import LazyImage from './LazyImage';

interface Props {
  backgroundImage: string;
  summary: string;
  title: JSX.Element;
  actionComponent: JSX.Element;
}

export const HeroSection = ({ backgroundImage, summary, title, actionComponent }: Props) => {
  return <div></div>;
};
